# OpenCode (opencode.ai) — OpenAI-compatible endpoint serving DeepSeek
OPENCODE_API_KEY=your_opencode_api_key_here
OPENCODE_BASE_URL=https://opencode.ai/zen/v1   # no /chat/completions suffix
MODEL_NAME=deepseek-v4-flash   # requires credits; see README

# Local ONNX embedding model (~50MB, downloads once). No API key needed.
EMBED_MODEL=BAAI/bge-small-en-v1.5
